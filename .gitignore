# Host-local legacy runner config. Remove this rule with the legacy Compose
# implementation after the live cutover; never commit its credential content.
.env

# Generated release and scheduled-monitor evidence.
dist/
dependency-drift.json
