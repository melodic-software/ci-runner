# No supported workflow writes a .env here, but one carried App credentials in
# the retired Compose era — keep it unstageable in this public repository.
.env

# Generated release and scheduled-monitor evidence.
dist/
dependency-drift.json
